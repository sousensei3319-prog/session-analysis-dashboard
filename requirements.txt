# Oracle本番(Python3.10)+CI(Python3.11)で稼働実証済みのバージョンに固定する。
# 未固定だとStreamlit Cloudが最新(pandas3.0.3/numpy2.5.1/pyarrow25)を引き、
# ネイティブ層のSegmentation faultを起こす(2026-07-12にCloud実機で確認)。
streamlit==1.59.1
plotly==6.9.0
pandas==2.3.3
numpy==2.2.6
pyarrow==24.0.0
ccxt==4.5.64
yfinance==1.5.1
openpyxl==3.1.5
